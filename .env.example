# Copy to .env for local (non-Docker) use. Compose injects these itself.
DATABASE_URL=postgresql://flags:flags@localhost:5432/flags
AUTH_SECRET=replace-with-openssl-rand-base64-32
AUTH_TRUST_HOST=true
